import React from 'react';

export const HeroPanel: React.FC<any> = () => {
    return (
        <div style={{ display: 'none' }}>
            {/* The giant Hero panel is deprecated in favor of the compact metric cards in RateStrip, 
                as per Phase 4 compact serious dark mode dashboard requirement. */}
        </div>
    );
};
